import 'dart:ui';

class GroupModel{
  String title;
  String groupIconPath;
  Color backgroundIcon;
  Color numberColor;
  String number;

  GroupModel({required this.title,required this.groupIconPath,required this.backgroundIcon,required this.number,required this.numberColor});
}